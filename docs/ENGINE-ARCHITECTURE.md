# Engine Architecture & Data Flow

> **Canonical:** [`nebulith/docs/ENGINE-ARCHITECTURE.md`](../../nebulith/docs/ENGINE-ARCHITECTURE.md)
> — the current engine architecture (one grid → three renders), data model (`IsometricGrid`), render
> pipeline, DB-driven tilesets, generators, editor/runtime, persistence + Elixir backend, with mermaid
> system/data-flow diagrams. Read with [`MAP-MODEL.md`](./MAP-MODEL.md) + [`FEATURES.md`](./FEATURES.md).
>
> This engine repo is where the code lives; the canonical doc references these `src/*` paths directly.
> Keep the two in sync. Standing flow: **check docs → understand → do the work.**
